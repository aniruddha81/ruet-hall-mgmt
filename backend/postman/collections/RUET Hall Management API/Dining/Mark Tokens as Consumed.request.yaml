$kind: http-request
url: "{{BASE_URL}}/api/dining/tokens/mark-consumed"
method: PATCH
headers:
  Authorization: Bearer {{ACCESS_TOKEN}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "tokenIds": [
        "token-uuid-1",
        "token-uuid-2",
        "token-uuid-3"
      ]
    }
order: 14000
